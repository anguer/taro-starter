// 页面配置，参考：https://docs.taro.zone/docs/page-config
export default definePageConfig({
  // 导航栏背景颜色，如 #000000
  // navigationBarBackgroundColor: '#fff',
  // 导航栏标题颜色，仅支持 black / white
  // navigationBarTextStyle: 'black',
  // 导航栏标题文字内容
  navigationBarTitleText: '首页',
  // 导航栏样式，仅支持以下值：default 默认样式；custom 自定义导航栏，只保留右上角胶囊按钮
  // navigationStyle: 'default',
  // 导航栏透明设置。支持 always 一直透明 / auto 滑动自适应 / none 不透明，仅支付宝小程序支持
  // transparentTitle: 'none',
  // 窗口的背景色
  // backgroundColor: '#ffffff',
  // 下拉 loading 的样式，仅支持 dark / light
  // backgroundTextStyle: 'light',
  // 是否开启当前页面的下拉刷新
  // enablePullDownRefresh: false,
  // 页面上拉触底事件触发时距页面底部距离，单位为 px
  // onReachBottomDistance: 50,
  // 设置为 true 则页面整体不能上下滚动。只在页面配置中有效，无法在 app.json 中设置
  // disableScroll: false,
  // 禁止页面右滑手势返回
  // disableSwipeBack: false,
  // 是否启用分享给好友
  // enableShareAppMessage: false,
  // 是否启用分享到朋友圈
  // enableShareTimeline: false,
  // 页面自定义组件配置
  // usingComponents: {},
})
